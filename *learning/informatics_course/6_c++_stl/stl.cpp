#include <iostream>
#include <vector>
using namespace std;

int main(){
    /*
        swap(a, b);
        min_element(begin, end);
        max_element(begin, end);
        reverse(begin, end);
        rotate(first, n_first, last); n_first на 0 индекс переходит, n_first-1 на последний

        слить два вектора:
        vector<int> res;
        merge(v1.begin, 1.end, v2.begin, v2.end, res);

        оставить в векторе только уникальные элементы:
        v.resize(unique(v.begin(), v.end()) - v.begin())

        для отсортированных векторов:
        binary_search(begin, end, val);
        lower_bound(begin, end, val); указатель на эл. >= val
        upper_bound(begin, end, val); указатель на эл. > val
        для set/map есть свои более эффективные lower/upper_bound
    */

    return 0;
}